class Mock {
    constructor(size, index) {
        this.size = size;
        this.index = index;
        this.list = [];
    }

    result = {
        items: [],
        total: 0,
    }

    getData = () => {
        // if(this.size ===)
    }
}

export default Mock